## How to use

+ `make tags` to generate files with tags depending on whether we want to use python for tag validation or not.
+ `make accounts` for account completion in emacs.

I have tested with [ledger cli](https://ledger-cli.org/) `v3.4.1` when compiling with python support.
